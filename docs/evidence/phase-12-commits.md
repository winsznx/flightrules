# Phase 12 commits

Branch `phase/12-ui-foundation`, cut from `1d3d1bb` on `main`.

| Commit | Subject |
|---|---|
| (this branch) | `feat(web): build the product shell and every PRD section 8 route on design.md's tokens` |

The merge commit is appended once `main` is green.
