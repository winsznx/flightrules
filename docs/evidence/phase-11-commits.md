# Phase 11 commits

Branch `phase/11-release-gate`, cut from `a48f261` on `main`.

| Commit | Subject |
|---|---|
| `a48f261` | `docs(evidence): record the verified Phase 10 baseline and the Phase 11-13 batch plan` (on `main`, before the branch) |
| `e40b920` | `feat(release-gate): aggregate a release into a deterministic gate decision, a CLI and a CI gate` |

The merge commit and the post-phase documentation commit are appended below once `main` is green.
